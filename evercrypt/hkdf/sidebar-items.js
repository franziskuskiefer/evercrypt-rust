initSidebarItems({"fn":[["expand","HKDF expand using hash function `mode`, pre-key material `prk`, `info`, and output length `okm_len`. Returns the key material in a vector of length `okm_len`."],["extract","HKDF extract using hash function `mode`, `salt`, and the input key material `ikm`. Returns the pre-key material in a vector of tag length."],["hkdf","HKDF using hash function `mode`, `salt`, input key material `ikm`, `info`, and output length `okm_len`. Calls `extract` and `expand` with the given input. Returns the key material in a vector of length `okm_len`."]]});